use glyph_brush_layout::{SectionGeometry, Layout};

pub struct Section {

}

impl Section {
    pub fn new() -> Self {
        let fonts = 
        let layout = Layout::default()

        Self {

        }
    }
}